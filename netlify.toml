[build]
  base = "."
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/privacy-policy"
  to = "/privacy-policy/"
  status = 301

[[redirects]]
  from = "/terms-of-service"
  to = "/terms-of-service/"
  status = 301

[[redirects]]
  from = "/disclaimer"
  to = "/disclaimer/"
  status = 301

[[headers]]
  for = "/*"
  [headers.values]
  Strict-Transport-Security = "max-age=86400"
  X-Content-Type-Options = "nosniff"
  X-Frame-Options = "SAMEORIGIN"
  Referrer-Policy = "strict-origin-when-cross-origin"
  Permissions-Policy = "geolocation=(), microphone=(), camera=()"

# Netlify Forms configuration
# Email notifications need to be configured in Netlify Dashboard:
# Site settings > Forms > Form notifications > Add notification
# Set recipient email to: WAELAMER@AMERLAWLLC.COM or mjamilamer@gmail.com
#
# Custom Email Subject Line:
# In the notification settings, you can use form field placeholders in the subject:
# - {{name}} - Submitter's name
# - {{email}} - Submitter's email
# - {{practice-area}} - Selected practice area
# - {{phone}} - Phone number
#
# Example subject templates:
# - "New Contact: {{name}} - {{practice-area}}"
# - "Contact Form: {{name}} ({{email}})"
# - "{{practice-area}} Inquiry from {{name}}"
#
# Or use the simple subject: "New Contact Form Submission from Amer Law Website"
# (Configured in src/utils/constants.js - FIRM.email.contactFormSubject)
